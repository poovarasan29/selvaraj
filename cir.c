#include<stdio.h>

int main()
{

    int rad,b,h,l,w;
    float PI = 3.14, area, ci;

    printf("\nEnter radius of circle: ");
    scanf("%d", &rad);

    area = PI * rad * rad;
    printf("\nArea of circle : %f ", area);

    ci = 2 * PI * rad;
    printf("\nCircumference : %f ", ci);
     Rec=l*w;
    printf(" \nrectangle :%f",area);
    Tri=l*b/2;
    printf("\ntringle :%f",area);
    return 0;
}


s://goo.gl/D2iETN
